/**
* Class that represents a perspective
*/
function xmlPerspective(id, near, far, angle, arrayFrom, arrayTo)
{
  this.id = id;
  this.near = near;
  this.far = far;
  this.angle = angle;
  this.from = arrayFrom;
  this.to = arrayTo;
};
